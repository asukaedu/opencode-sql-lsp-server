# Changelog

## 0.1.0

- Initial VS Code wrapper for launching `opencode-sql-lsp --stdio`
- Adds configurable server path and server arguments
- Packages as a standalone `.vsix` under the wrapper-local `dist/` directory
